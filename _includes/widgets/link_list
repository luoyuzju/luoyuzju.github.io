<div class="widget">
    <div class="widget-title"><i class="icon-category">友情链接</i></div>
        {% for link in site.links %}
            <a href="{{ site.baseurl }}/{{ menu.directory }}"><i class="{{ menu.icon }}"> {{ menu.page }}</i></a>
            <ul></ul>
            <a href="{{ link.url }}" title="{{ link.title }}" target="_blank">{{ link.title }}</a>
        {% endfor %}
</div>